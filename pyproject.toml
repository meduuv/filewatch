[build-system]
requires=["setuptools>=69"]
build-backend="setuptools.build_meta"
[project]
name="filewatch"
version="0.1.0"
description="Lightweight filesystem change monitor"
requires-python=">=3.10"
[project.scripts]
filewatch="filewatch.cli:main"
[tool.setuptools.packages.find]
where=["src"]
